version: '3.8'

services:
  mines-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mines-api
    restart: unless-stopped
    ports:
      - "9598:9598"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:9598/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
