{
  "name": "spinwheel",
  "version": "1.0.0",
  "description": "Spin Wheel Game on Monad Testnet",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network monad",
    "start": "node index.js",
    "spinwheel": "hardhat run scripts/all-in-one.js --network monad",
    "build": "echo 'No build process necessary. This is a root package.json for project organization.'"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.1",
    "@openzeppelin/contracts": "^4.9.6",
    "ethers": "^5.8.0",
    "mongoose": "^8.15.1",
    "react-router-dom": "^7.5.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.5.0",
    "hardhat": "^2.17.0"
  }
}
